export interface WorkMetadata {
    title: string;
    tags: string[];
    description: string;
    shortDescription?: string;
    previewImage?: string;
    demoUrl?: string;
    repoUrl?: string;
    date: string;
    techStack: string[];
}
